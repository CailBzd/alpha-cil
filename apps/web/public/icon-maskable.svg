<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512">
  <defs>
    <linearGradient id="g" x1="0" y1="0" x2="1" y2="1">
      <stop offset="0" stop-color="#4f46e5" />
      <stop offset="1" stop-color="#10b981" />
    </linearGradient>
  </defs>
  <!-- Full-bleed, no corner rounding: the OS applies its own mask shape
       (circle/squircle/rounded-square), so the source must fill the
       canvas edge to edge. The glyph stays within the ~80% safe zone
       (roughly x/y 102-410) so it survives any mask shape. -->
  <rect width="512" height="512" fill="url(#g)" />
  <path
    d="M256 154 L346 226 V336 a12 12 0 0 1 -12 12 H302 v-72 h-92 v72 H166 a12 12 0 0 1 -12 -12 V226 Z"
    fill="#ffffff"
  />
</svg>
