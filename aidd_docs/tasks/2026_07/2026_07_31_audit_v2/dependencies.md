## dependencies

Clean: `corepack pnpm audit` reports zero known vulnerabilities and `corepack pnpm why sharp`/`corepack pnpm why postcss` each resolve to exactly one version workspace-wide (`sharp@0.35.3`, `postcss@8.5.23`) — the four CVEs from the pinned `next` sub-dependencies are gone, and the resend/lucide-react/tailwind-merge bumps genuinely landed in `pnpm-lock.yaml` (not just as `package.json` ranges), each confirmed via `corepack pnpm why <pkg>` showing a single resolved version at the claimed target.

| Sev | Category | Location | Issue | Suggested fix | Effort |
| --- | --- | --- | --- | --- | --- |
| 🟢 | dependencies | `pnpm-workspace.yaml` | Verified: `overrides: { postcss: ">=8.5.18", sharp: ">=0.35.3" }` resolves cleanly — `pnpm-lock.yaml` shows exactly one entry each, `postcss@8.5.23` and `sharp@0.35.3`, no lingering `8.4.31`/`0.34.5` anywhere in the resolved graph (a stale `sharp@0.34.5`/`@img+sharp-win32-x64@0.34.5` directory remains in the local `.pnpm` store cache from before the override, but it is orphaned — not referenced by the lockfile or `pnpm why sharp`, and disappears on a clean install/prune). `pnpm audit` is clean. | No action; optionally `pnpm store prune` locally to drop the orphaned cache entry, purely cosmetic. | S |
| 🟢 | dependencies | `packages/notifications/package.json` | Verified: `resend` is genuinely at `6.18.1` in the lockfile (`pnpm why resend` shows one version). It pulled two new transitive deps not present at `4.8.0`: `postal-mime@2.7.5` (MIT-0) and `standardwebhooks@1.0.0` (MIT) — both permissive licenses, no advisories against either in `pnpm audit`. | No action needed. | S |
| 🟢 | dependencies | `apps/web/package.json`, `packages/ui/package.json` | Verified: `lucide-react` genuinely at `1.28.0`, `tailwind-merge` genuinely at `3.6.0` in the lockfile, each a single resolved version workspace-wide, both already at the current npm `latest` tag (checked via `pnpm view`). No new transitive deps or license changes introduced by either bump. | No action needed. | S |
| 🟡 | dependencies | `apps/web/package.json` | Unchanged from prior audit: `next` still one major behind (`15.5.21` installed vs `16.2.12` latest) — deliberately deferred, real App Router migration risk. Confirmed still the single source pinning `postcss`/`sharp` transitively (both now patched via the workspace override). | Keep deferred to a dedicated Next 16 migration pass. | M |
| 🟢 | dependencies | every workspace `package.json` (`devDependencies.typescript`) | Unchanged: `typescript@5.9.3` vs `7.0.2` latest (native/"Corsa" rewrite), dev-only. | Defer until TS7 tooling ecosystem matures. | S |
| 🟢 | dependencies | `apps/web/package.json` (`react-leaflet`) | Unchanged: `react-leaflet`/`@react-leaflet/core` still ship under Hippocratic-2.1 (non-OSI). Re-confirmed present and unaltered by this session's bumps. Deliberately left open pending legal review, per prior audit. | No code action; still needs a legal decision. | S |
| 🟢 | dependencies | `packages/db/package.json`, root `package.json`, `apps/web/package.json` | Same trivial patch bumps still available, one new one surfaced: `@supabase/supabase-js` 2.110.8→2.111.0, `@supabase/ssr` 0.12.3→0.12.4, `turbo` 2.10.6→2.10.7, `postcss` (dev) 8.5.23→8.5.25, and newly noted `@types/node` 22.20.1→26.1.2 (crossed a major, dev-only, no runtime risk — matches Node's release cadence reaching v26). | Bundle into a routine patch-refresh PR; treat `@types/node` major bump like the `typescript` one (defer, no urgency). | S |
| 🟢 | dependencies | `apps/web/package.json` (`devDependencies`) | New test-infra additions `vitest@4.1.10` and `@playwright/test@1.62.1`, audited for the first time this session: both are already at the current npm `latest` tag (verified via `pnpm view`), no known vulnerabilities in `pnpm audit`, no non-standard licenses. | No action needed — genuinely clean and current. | S |

No new critical or warning-severity findings emerged from this re-audit. All four items from the 2026-07-31 same-day fix pass (postcss/sharp overrides, resend, lucide-react, tailwind-merge) are confirmed genuinely resolved in `pnpm-lock.yaml`, not merely bumped in `package.json` ranges.
