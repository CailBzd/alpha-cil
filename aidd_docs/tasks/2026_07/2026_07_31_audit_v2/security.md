## security

Health: good — no critical or new high-severity issues found on this re-audit. Both 0034 (`match_or_create_logement` artisan-only guard) and 0035 (`artisans` column-level update lock) hold up under a skeptical direct read of the SQL and cross-checked against every real call site: the guard in 0034 sits before all four branches so nothing in the function is reachable by a non-artisan, and 0035's column list exactly matches what `SocieteForm.tsx`/`decennale/route.ts` write and excludes `id`/`siret`/`created_at` — both fixes are verified sound, not just claimed.

| Sev | Category | Location | Issue | Suggested fix | Effort |
| --- | --- | --- | --- | --- | --- |
| 🟢 | security | `supabase/migrations/0034_match_or_create_logement_artisan_only.sql:42-45` | Verified sound: the `exists (... a.id = auth.uid())` guard is the first statement in the function body and returns immediately, so it gates all four branches (no-match/insert, ambiguous, already-claimed/email-leak, unclaimed-update) with no bypass path. Cross-checked against `apps/web/test/integration/match-or-create-logement.integration.test.ts`, which asserts a non-artisan gets all-nulls and an artisan still gets the real data through every branch. No action needed. | — | — |
| 🟢 | security | `supabase/migrations/0035_artisans_update_self_column_lock.sql:10-15` | Verified sound: grant list (`denomination, adresse, telephone, corps_metier, attestation_decennale_path, attestation_decennale_uploaded_at`) exactly matches the two real write paths — `apps/web/app/(artisan)/artisan/espace/profil/SocieteForm.tsx:44-49` and `apps/web/app/api/artisan/decennale/route.ts:46-51` — and correctly excludes `id`, `siret`, `created_at`. `siret` can now only be set once, at insert, via `createSiretAccount` (`apps/web/lib/siret-signup.ts:55-57`). No action needed. | — | — |
| 🟡 | security | `supabase/migrations/0008_rattachement_logement.sql:96`, unchanged by 0034 | `match_or_create_logement` (and its predecessor/current redefinition) has only ever been explicitly granted EXECUTE to `authenticated` — never to `public`/`anon` — but Postgres auto-grants EXECUTE to `PUBLIC` on function creation unless explicitly revoked, and this function's `create or replace` in 0034 never added a `revoke execute ... from public`, unlike the repo's own established convention for security-sensitive functions (`logements_a_notifier_entretien` in 0016/0021, `logement_exists_for_adresse` in 0031). This is very likely still dormant/harmless in practice — the 0034 guard checks `auth.uid()`, which is NULL for an anon-role call with no user JWT, so an anon caller still gets all-nulls regardless of the grant — but it's a latent inconsistency with the codebase's own hardening pattern and worth closing for defense-in-depth (a future refactor of the guard that relied on role membership instead of `auth.uid()` would silently reopen this). | Add `revoke execute on function public.match_or_create_logement(text, text, text, text, numeric, numeric, date, numeric) from public;` in a follow-up migration, matching the 0016/0021/0031 convention. | S |
| 🟢 | security | `apps/web/lib/supabase-server.ts:11-27`, `apps/web/lib/siret-signup.ts:21-65` | Verified no regression from the code-quality extraction (commit `27906bb`): diffed the new shared `getServerSupabaseClient`/`createSiretAccount` helpers against the pre-refactor artisan/agence signup routes (commit history `6a9c31c`→`27906bb`) — SIRET verification before `signUp`, the `user_already_exists`/"already registered" duplicate-email handling, and the profile-row insert are all preserved byte-for-byte in logic for both personas. The added try/catch around `cookieStore.set` is a documented no-op for Route Handlers (cookie writes succeed there) and the correct Server Component escape hatch — no auth-cookie behavior change. No action needed. | — | — |
| 🟢 | security | `supabase/migrations/0036_foreign_key_indexes.sql` | Confirmed index-only as claimed: 12 `create index if not exists` statements on 8 tables' foreign-key columns, no policy, grant, or function changes. Zero security impact. No action needed. | — | — |
| 🟡 | security | `supabase/migrations/0025_devis_projets.sql:77-86` | Still open, unchanged since the 2026-07-31 audit (confirmed by direct read — not touched by any of the 0034-0036 fixes): `devis_insert_owner` checks the inserting owner controls `projet_id` but never validates that `contact_id` also belongs to that same owner, so an owner can insert a `devis` row pointing at another owner's `contact_id`. Same gap likely applies to `devis_update_owner` a few lines below. Left out of scope for this pass per the original audit's note; flagging again so it isn't lost. | Add `and exists (select 1 from public.contacts c where c.id = devis.contact_id and c.proprietaire_id = auth.uid())` to the `with check` clause of both `devis_insert_owner` and `devis_update_owner`. | S |
| 🟢 | security | `apps/web/app/(artisan)/artisan/espace/layout.tsx:10-24` | Verified sound: centralizes the auth+persona guard (added in commit `6523715`) exactly matching the `agence`/`proprietaire` layouts — `getServerSupabaseClient().auth.getUser()` then `resolvePersona` redirect-on-mismatch, before rendering any child route. Sub-pages that used to duplicate this guard (`profil/page.tsx`, `interventions/nouvelle/page.tsx`, etc.) now correctly rely on the parent layout instead, which Next.js guarantees renders for every nested route — no bypass path found. No action needed. | — | — |
| 🟢 | security | `apps/web/app/api/cron/rappels-entretien/route.ts:30` | Verified sound: `!process.env.CRON_SECRET || authorization !== \`Bearer ${process.env.CRON_SECRET}\`` fails closed when the env var is unset — confirmed this is still the current logic, not reverted by any later commit. No action needed. | — | — |
