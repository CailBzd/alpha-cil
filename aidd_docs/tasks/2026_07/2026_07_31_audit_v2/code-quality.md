## code-quality

Good: the 2026-07-31 fixes landed cleanly with no leftover dead code, no half-migrated call sites, and no new duplication introduced by the extractions themselves — `getServerSupabaseClient` (42 call sites), `isNonEmptyString`/`isNonEmptyStringTrimmed` (13+ call sites), `formatters.ts`, `corpsMetierLabel`/`chauffageLabel`/`vmcLabel`, `createSiretAccount`, and `ConnexionForm` all checked out as fully swept and consistently used; the one remaining minor spot-check (`EspaceSidebar` duplication) is still present exactly as before, now with a third copy. The only real new debt is duplication the *same-day feature commits* (SIRET/corps-métier work) introduced in code the login-form and signup-route refactors didn't touch: the three persona signup **forms**.

| Sev | Category | Location | Issue | Suggested fix | Effort |
| --- | --- | --- | --- | --- | --- |
| 🟡 | code-quality | `apps/web/app/(public)/artisan/inscription/page.tsx:15-71`, `apps/web/app/(public)/proprietaire/inscription/ProprietaireInscriptionForm.tsx:22-66`, `apps/web/app/(public)/agence/inscription/AgenceInscriptionForm.tsx:24-85` | The three signup forms are ~90% identical: password/confirmation mismatch check, `fetch` POST to the persona's `/api/*/signup` route, the same `email_taken`/`siret_invalide`/`siret_indisponible` → French message mapping, and the same try/catch-to-generic-error fallback. This is the exact pattern `ConnexionForm` (`apps/web/app/ConnexionForm.tsx`) was extracted to solve for login — but that refactor covered sign-*in* only, leaving sign-*up* with the same duplication it fixed on the other side. Notably the SIRET-related branches were added independently to two of the three copies (artisan, agence) by the same-day 83663e8 commit, so the pattern has already started drifting rather than converging. | Extract a shared `InscriptionForm`/`useSignupSubmit({ endpoint, extraFields, errorMessages })` hook or component mirroring `ConnexionForm`'s shape; keep persona-specific fields (SIRET, corps de métier, invitation banner) as children/props. | M |
| 🟢 | code-quality | `apps/web/app/(public)/artisan/inscription/page.tsx:110-123` vs `apps/web/app/(artisan)/artisan/espace/profil/SocieteForm.tsx:71-86` | The corps-de-métier checkbox checklist (label + `CORPS_METIER_OPTIONS.map` + `<input type="checkbox" name="corpsMetier">`) is duplicated verbatim between artisan signup and the profile's `SocieteForm`, differing only in `defaultChecked`. Introduced by the same-day 83663e8 commit that added multi-trade support to both places at once. | Extract a small `CorpsMetierChecklist({ selected }: { selected?: string[] })` component from `@/lib/corps-metier` or a new `apps/web/app/CorpsMetierChecklist.tsx`, used by both. | S |
| 🟢 | code-quality | `apps/web/app/(owner)/proprietaire/espace/EspaceSidebar.tsx`, `apps/web/app/(agence)/agence/espace/EspaceSidebar.tsx`, `apps/web/app/(artisan)/artisan/espace/EspaceSidebar.tsx` | Spot-check of previously-deferred minor #18: still accurate, and now worse — the new artisan `layout.tsx` (`apps/web/app/(artisan)/artisan/espace/layout.tsx:34`) added a *third* byte-for-byte-identical `EspaceSidebar` component differing only in the hardcoded `LINKS` array, rather than reusing a shared one. | Make `EspaceSidebar` accept a `links` prop, delete the two extra copies. | S |
| 🟢 | code-quality | `apps/web/app/page.tsx:99-113` (header), `apps/web/app/page.tsx:260-270` (footer) | The homepage's persistent header/footer nav still only link "Espace artisan" and "Espace propriétaire" — the agence persona has no direct nav link anywhere on the page except the one persona card (line 42, itself only fixed today from a copy-paste bug). An agency user has to scroll to the mid-page card to find their login. | Add an "Espace agence" link next to the other two in both the header (~line 106) and footer (~line 267). | S |

Everything else checked was clean: `apps/web/lib/formatters.ts`, `apps/web/lib/form-validation.ts`, `apps/web/lib/supabase-server.ts`, `apps/web/lib/siret-signup.ts` and `apps/web/app/ConnexionForm.tsx` are each used consistently at every call site with no stray duplicates; the four row components (`RappelRow.tsx`, `ContactRow.tsx`, `DevisRow.tsx`, `RendezVousRow.tsx`) all now check `error` on delete/toggle and reset `loadingAction`; the new Vitest (`apps/web/app/api/*/route.test.ts`, `apps/web/test/integration/*`) and Playwright (`apps/web/e2e/persona-guard.spec.ts`) suites are well-scoped and well-commented, not just scaffolding.
