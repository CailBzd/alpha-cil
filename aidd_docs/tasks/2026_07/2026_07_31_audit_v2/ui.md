## ui

Good — the responsive-shell and artisan-nav fixes both hold up under a close read: all 3 layout.tsx files now share the identical `flex-col sm:flex-row` shell and all 3 `EspaceSidebar.tsx` files share the identical `overflow-x-auto` pill-nav-below-`sm` / `w-48` sidebar-above-`sm` pattern, artisan's sidebar genuinely uses `Link`, and dropping `profil`/`interventions/nouvelle`'s standalone chrome caused no regression since they're now plain fragments inheriting the shared shell; the real news this session is a new, systemic-but-minor issue — the error `Alert`s just added to 5 row components sit inside `flex-wrap` rows without the `w-full` their sibling elements use, so they don't reliably span their own line.

| Sev | Category | Location | Issue | Suggested fix | Effort |
| --- | --- | --- | --- | --- | --- |
| 🟡 | ui | `apps/web/app/(owner)/proprietaire/espace/contacts/ContactRow.tsx:143`, `.../rappels/RappelRow.tsx:170`, `.../projets/[projetId]/DevisRow.tsx:168`, `.../calendrier/RendezVousRow.tsx:135`, `apps/web/app/(artisan)/artisan/espace/rendez-vous/ArtisanRendezVousRow.tsx:105` | New this session: each row's error `<Alert>` was added as a bare flex child inside a `flex flex-wrap` `<li>`, with no `w-full` — unlike the sibling `notes`/`interlocuteur` spans in the same rows, which all carry `className="w-full"` specifically so they force their own line. Since `Alert` has `flex-grow: 0` and no explicit width, its box sizes to its content rather than the row width, so after a failed save/delete it can render squeezed onto the same line as the status pill or the action buttons instead of on its own full-width line, and its width will vary unpredictably depending on how much space is left over. | Add `className="w-full"` to the 5 `<Alert>` call sites in these row components (or wrap them in a `<div className="w-full">`), matching the existing `notes`/`interlocuteur` pattern. | S |
| 🟢 | ui | `apps/web/app/(owner)/proprietaire/espace/acces/RevokeButton.tsx:12-20` | Incidental regression in consistency (not severity): this session's row-error-handling pass (commit `6523715`) added error checking + an `Alert` to `RappelRow`, `ContactRow`, and `DevisRow`, but `RevokeButton` — one of the same 5 delete/revoke actions flagged in the prior audit — was left untouched. It still has no `window.confirm()` (still open, as before) and now is also the only one of the 5 with no error surfacing at all: `handleRevoke` doesn't check the Supabase response's `error` before calling `router.refresh()`, so a failed revoke still fails silently with the button stuck in its `loading` state reset regardless of outcome. | While adding confirmation to the 5 actions (still-open finding below), also apply the same error-check pattern used in `RappelRow`/`ContactRow`/`DevisRow` to `RevokeButton`. | S |
| 🟡 | ui | `apps/web/app/(owner)/proprietaire/espace/contacts/ContactRow.tsx:153-161`, `.../projets/[projetId]/DevisRow.tsx:177-186`, `.../rappels/RappelRow.tsx:188-198`, `.../calendrier/RendezVousRow.tsx:153-163`, `apps/web/app/(owner)/proprietaire/espace/acces/RevokeButton.tsx:22-26` | Still open, still accurately described: all 5 "Supprimer"/"Révoquer" actions fire on first click with no confirmation step, then `router.refresh()` with no undo. Unchanged since the prior audit. | Reuse the `window.confirm()` pattern already used in `ExportForm.tsx:31` on all 5 delete/revoke actions. | S |
| 🟡 | ui | `apps/web/app/(owner)/proprietaire/espace/calendrier/CalendrierList.tsx:131-151` | Still open, still accurately described: the "Interventions"/"Rappels"/"Rendez-vous" filter chip `<button>`s toggle a `Set` but carry no `aria-pressed`; state is conveyed purely by the gradient-fill vs. outline styling. Untouched by this session's refactors. | Add `aria-pressed={activeKinds.has(kind)}` to each chip button. | S |
| 🟢 | ui | `apps/web/app/(artisan)/artisan/espace/profil/SocieteForm.tsx:76-81`, `apps/web/app/(owner)/proprietaire/espace/EquipementsForm.tsx`, `.../acces/AccesForm.tsx:68-72,89-101,113`, `.../export/ExportForm.tsx:137-141,151-155`, `apps/web/app/(public)/artisan/inscription/page.tsx` | Still open, still accurately described: every checkbox/radio in the app remains a raw unstyled native `<input>` — no themed `Checkbox`/`Radio` exists in `packages/ui`. Line positions are effectively unchanged since the prior audit. | Add a themed `Checkbox`/`Radio` to `packages/ui` (or apply `accent-color: var(--color-primary)`) and swap call sites over. | S |
| 🟢 | ui | `apps/web/app/(artisan)/artisan/espace/profil/DecennaleForm.tsx:55-61`, `.../interventions/nouvelle/InterventionForm.tsx:51,72-78` | Still open, still accurately described: file-upload fields still reuse the shared `Input` (`h-9` height tuned for text), with no dedicated `FileInput` and no visible max-size/accepted-type hint beyond label text. Unchanged since the prior audit. | Add a lightweight `FileInput` variant to `packages/ui`. | S |

Verification notes on what's genuinely fixed (not re-flagged):
- All 3 shells (`apps/web/app/(owner)/proprietaire/espace/layout.tsx:33`, `apps/web/app/(agence)/agence/espace/layout.tsx:33`, `apps/web/app/(artisan)/artisan/espace/layout.tsx:33`) use the identical `mx-auto flex max-w-5xl flex-col gap-6 px-6 py-8 sm:flex-row sm:gap-8` — the breakpoint logic is sound and consistent across all three personas, no divergence found.
- All 3 `EspaceSidebar.tsx` files use the identical `flex gap-1 overflow-x-auto pb-1 sm:w-48 sm:shrink-0 sm:flex-col sm:overflow-visible sm:space-y-1 sm:pb-0` nav, built on Next `Link` + `usePathname()`, not `<a href>` — artisan's is no longer hand-duplicated or diverging from owner/agence.
- Content-heavy components (`FinancesCharts.tsx`'s two `ResponsiveContainer` recharts, the `flex-wrap` row lists in `RendezVousRow.tsx`, `DevisRow.tsx`, `ArtisanRendezVousRow.tsx`, artisan's `page.tsx` intervention list) all reflow correctly now that they receive full viewport width below `sm` instead of the ~100px sliver from before; no table/chart-specific overflow found.
- `profil/page.tsx` and `interventions/nouvelle/page.tsx` (artisan) are now plain fragments relying on the parent layout's `max-w-5xl` shell — no orphaned `max-w-lg`/`mx-auto` wrapper left behind, no visual regression from removing their standalone chrome.
- The new `ConnexionForm.tsx` (`apps/web/app/ConnexionForm.tsx`) renders its error/blocked `<Alert>`s correctly — it's a plain vertical `space-y-4` form, not a `flex-wrap` row, so the `w-full`-omission issue above doesn't apply there.
