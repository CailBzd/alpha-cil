## performance

Health is good and improving: all three same-day fixes (FK indexes, gov-API fetch timeouts, parallelized DPE/RGE lookups) were re-derived independently from the current schema/code and landed correctly with no errors, races, or omissions — the one fresh gap is a client-side geocoding fetch (`LogementMap.tsx`) that was never in scope of the original fix and still has no timeout.

| Sev | Category | Location | Issue | Suggested fix | Effort |
| --- | --- | --- | --- | --- | --- |
| 🟡 | performance | `apps/web/app/(owner)/proprietaire/espace/LogementMap.tsx:29-31` | Client-side `fetch` to `https://api-adresse.data.gouv.fr/search/` has no `AbortSignal.timeout(...)` (unlike the 3 server-side gov-API calls that were just fixed). A slow/hung BAN response leaves the map stuck on "Localisation en cours…" indefinitely — no user-visible timeout path, only the `.catch` for outright network errors. | Add `signal: AbortSignal.timeout(5000)` to this fetch, same as `sirene.ts`/`ademe.ts`/`dpe.ts`; the existing `cancelled` flag already handles the unmount race, timeout just bounds the hang. | S |
| 🟢 | performance | `supabase/migrations/0036_foreign_key_indexes.sql` (verification, no code change needed) | Re-derived the full FK list from all 36 migrations by hand (every `references`/`on delete` occurrence across `0001`-`0035`): `logements.proprietaire_id`, `logement_invitations.logement_id`, `interventions.artisan_id`/`logement_id`, `logement_access_grants.logement_id`/`agence_id`, `logement_access_grant_interventions.intervention_id` (its `grant_id` is correctly left un-indexed — it's the leading PK column), `contacts.proprietaire_id`, `rappels.logement_id`/`contact_id`, `projets.logement_id`, `devis.projet_id`/`contact_id`, `rendez_vous.logement_id`/`contact_id`/`artisan_id`. Every one of these is covered by an `if not exists` index in 0036, with matching table/column names against the real schema (e.g. confirmed `devis` has no `logement_id` column — only `projets` does — so the original claim's phrasing was slightly imprecise but 0036 itself is exactly right). No missing FK index found. | None — informational, confirms the fix is complete and correct. | — |
| 🟢 | performance | `apps/web/app/api/artisan/interventions/route.ts:76-90` | Confirmed the `Promise.all([lookupDpe(...), verifyRge(...)])` is safe: `dpe` is only read at lines 84-89 (RPC args) and again at 134-138 (insert columns), `rgeVerifie` only at line 126 — both reads happen after the `Promise.all` resolves, no partial/early use of either value, no shared mutable state between the two calls. No race condition. | None. | — |
| 🟢 | performance | `apps/web/app/api/rendez-vous/route.ts:75`, `apps/web/app/api/rendez-vous/[id]/route.ts:80,87`, `apps/web/app/api/proprietaire/acces/route.ts:91`, `apps/web/app/api/artisan/interventions/route.ts:156` | Pre-existing pattern (not a regression from this session): every notification email is `await`-ed before the API responds, even though it's wrapped in `.catch(() => {})` and documented as non-blocking for correctness — it still adds Resend's full round-trip latency to the user-facing response. Low severity at near-zero traffic; Resend SDK has its own internal timeout. | If it ever becomes noticeable, drop the `await` (fire-and-forget) or move to a queue/background job — not urgent pre-launch. | S |
| 🟢 | performance | `apps/web/app/api/cron/rappels-entretien/route.ts:44-98` | Sequential `for` loop sends up to 3 emails + 1 DB update per logement, one logement at a time — fine at today's volume but will approach Vercel's function duration limit if the notifiable-logement count grows meaningfully, since this is a scheduled cron job, not a request users wait on. | Batch with `Promise.all` (or `Promise.allSettled` to preserve the per-logement isolation) once logement counts grow; no action needed now. | M |
| 🟢 | performance | `apps/web/app/(owner)/proprietaire/espace/finances/FinancesCharts.tsx` and 5 list pages under `proprietaire/espace/*` | Confirmed unchanged from the 2026-07-31 audit's "left open" list: `recharts` still imported directly (not wrapped in `next/dynamic`), and list pages (`interventions`, `projets`, `contacts`, `calendrier`, `acces`, etc.) still fetch unpaginated. Genuinely low-risk pre-launch given near-zero traffic; re-confirmed present, not worsened. | Wrap `FinancesCharts` in `next/dynamic({ ssr: false })`; add `.range()`/cursor pagination to list queries once row counts grow. | S/M |
