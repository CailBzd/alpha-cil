## tests

Good and genuinely earned — 39 tests across 3 real suites (unit/integration/e2e) cover the previously-🔴 security paths with meaningful negative-case assertions, not happy-path theater; all three suites were actually executed against this checkout just now and all 39 pass (16 unit via `vitest run`, 14 integration via `vitest run --config vitest.integration.config.ts` against the running local Supabase instance, 9 e2e via `playwright test` against a real dev server + Supabase). Remaining gaps are the ones the brief already flagged as deliberately out of scope, plus two fresh ones below tied to this session's refactor (`createSiretAccount` and `ConnexionForm`'s blocked-message branch).

| Sev | Category | Location | Issue | Suggested fix | Effort |
| --- | --- | --- | --- | --- | --- |
| 🟡 | tests | `apps/web/app/(public)/agence/connexion/page.tsx:16-26` + `apps/web/app/ConnexionForm.tsx:57-64` (`blockedMessage` branch) | `ConnexionForm` is now shared across all 3 persona connexion pages (new this session), and agence's `afterSignIn` maps all 5 non-success `claim_agence_grant` reasons (`wrong_type`/`expired_or_revoked`/`email_mismatch`/`claimed_by_other`/`not_found`) to a user-facing French message shown instead of redirecting. The underlying RPC's 6 branches are well covered at the DB layer (`claim-agence-grant.integration.test.ts`), but nothing exercises this UI mapping layer — the e2e persona-guard matrix only signs in agence with no `?token=` param (the `if (!token) return;` early-out), so it never reaches the `CLAUDE_ERROR_MESSAGES` lookup or renders the blocked `Alert`/`blockedFooter`. There's also no component-testing tool installed at all (no `@testing-library/react`, no jsdom environment in `vitest.config.ts`), so today `ConnexionForm` itself is only reachable through Playwright. | Add one e2e case: sign in as agence with an expired/already-claimed `?token=`, assert the mapped message renders and `blockedFooter`'s "Continuer vers mon espace" link still works. Cheap since the fixtures (`makeGrant` with `expiresAt`/`revokedAt`) already exist in `test/integration/helpers.ts`. | S |
| 🟢 | tests | `apps/web/lib/siret-signup.ts` (`createSiretAccount`) | This is the single choke point for both artisan and agence signup (a genuine "more central" module per the brief), but has no dedicated `siret-signup.test.ts` — it's only exercised indirectly through `app/api/artisan/signup/route.test.ts` and `app/api/agence/signup/route.test.ts`. In practice this indirect coverage is thorough (invalid SIRET, SIRET-service-unavailable, duplicate email, post-signUp insert failure, success, plus both of agence's `afterInsert` claim outcomes all pass through it across the two files), so this is a test-organization nit, not a real coverage hole. | Optional: extract a `lib/siret-signup.test.ts` calling `createSiretAccount` directly with a fake Supabase client, mostly to shorten the two route test files rather than to add coverage. | S |
| 🟢 | tests | `apps/web/lib/supabase-server.ts:11-27` (`getServerSupabaseClient`) | New shared cookie adapter (dedupes what was previously copy-pasted across 34+ files) has no direct test, including the documented no-op `catch` for Server Components that can't write cookies. Low risk — it's a thin pass-through with no branching logic beyond that one intentionally-silent catch, and every route test that mocks `next/headers`/`@alpha-cil/db` already exercises the call path. | Skip unless the adapter grows real logic; not worth a dedicated test today. | S |
| 🟡 | tests | `packages/notifications/src/mailer.ts` call sites — now 6 routes (`apps/web/app/api/rendez-vous/route.ts`, `.../[id]/route.ts`, `.../proprietaire/acces/route.ts`, `.../artisan/invitation/route.ts`, `.../artisan/interventions/route.ts`, `.../cron/rappels-entretien/route.ts`) | Same gap as the 2026-07-31 audit, but the blast radius grew: 2 call sites then, 6 now (rendez-vous confirmation emails and the cron reminder loop were added since). Still zero test of the differing failure-handling contracts — e.g. the cron route's per-notification `try/catch` (route.ts:44-97) swallows a `sendMail` failure and skips the `rappel_..._envoye_a` update (correct, will retry next run), but if `sendMail` succeeds and the *subsequent* `.update()` fails, nothing catches that and the reminder would resend indefinitely — this specific interaction is unverified in either direction. | Unit test the cron route with `sendMail` mocked to throw (assert no `envoye_a` update, i.e. safe retry) and mocked to succeed with `.update()` mocked to fail (assert the resend-forever risk, then decide if it needs fixing). Same pattern applies to the 5 other call sites at lower priority. | S |
| 🟢 | tests | `apps/web/test/integration/claim-agence-grant.integration.test.ts:6-9` (own comment) | The suite's header comment explicitly calls out that the concurrent double-claim race (the reason the SQL function takes a `for update` row lock) is not covered — confirmed accurate by reading the test file; every case is sequential. Carried forward from the prior audit, now precisely scoped by the test author. | Add one test firing two `claim_agence_grant` calls concurrently (`Promise.all`) for the same token and asserting exactly one succeeds. | S |
| 🟡 | tests | `packages/intervention/src/sirene.ts`, `packages/intervention/src/ademe.ts`, `packages/logement/src/dpe.ts` | Unchanged from the 2026-07-31 audit: all three still call live gov endpoints directly via bare `fetch` with no injection seam or fixtures anywhere in the repo. Confirmed no new mocking layer was added this session (`grep fetch\(` in `packages/` still only hits these 3 files). Any new unit test that needs `verifySiret`'s real implementation (as opposed to the route tests, which mock it entirely) has no way to avoid the network. | Same fix as before: inject `fetch`/a client, record fixture JSON per endpoint (nominal/empty/malformed/timeout). | M |
| 🟡 | tests | RLS policies overall — `supabase/migrations/*.sql`, now 36 files (3 new since the last audit: `0034_match_or_create_logement_artisan_only.sql`, `0035_artisans_update_self_column_lock.sql`, `0036_foreign_key_indexes.sql`) | Still no pgTAP/SQL-level harness; 0034 is itself a security fix (an artisan-only guard added after `match_or_create_logement` was found reachable by any authenticated role) and is well covered by the new integration test's first case, but 0035 (a column-lock policy on `artisans` self-updates) has no direct test anywhere — worth checking it isn't silently bypassable. | Same as before: stand up `supabase test db` (pgTAP), starting with 0035 since it's new and unverified. | L |
| 🟢 | tests | project-wide — still no CI (`.github/` absent, confirmed via `find .github` returning nothing) | Unchanged from the prior audit. All 39 tests passing locally right now means nothing until something runs them on every push/PR — a regression could still reach `main` today with zero automated gate. | Add a minimal GitHub Actions workflow: `pnpm build`/`pnpm lint`/`pnpm test` (unit only, since integration needs a live Supabase — consider `supabase start` as a CI step or defer that tier to a nightly job). | S |
| 🟢 | tests | `apps/web/app/api/proprietaire/export/pdf/route.ts`, `apps/web/app/api/cron/rappels-entretien/route.ts` | Both unchanged from the prior audit and still untested: PDF export content/access, and cron idempotency (`logements_a_notifier_entretien` RPC scoping + the resend-on-partial-failure risk noted above). | Same suggested fixes as the 2026-07-31 report. | S |

Full detail on unchanged items (RLS pgTAP, sirene/ademe/dpe fixtures, CI, PDF export, cron) is in `aidd_docs/tasks/2026_07/2026_07_31_audit/tests.md` — re-verified still accurate rather than re-derived from scratch.
