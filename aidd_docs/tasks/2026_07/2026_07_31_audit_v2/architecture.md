## architecture

Health is good and improving — the artisan layout extraction, the `RendezVousRow` delete fix, and the INSTALL.md correction all landed cleanly with no loose ends, and the new shared modules (`supabase-server.ts`, `siret-signup.ts`, `ConnexionForm.tsx`) are used consistently everywhere they should be; the one fresh finding is a mutating RPC that slipped past the write-path convention documented in the same sweep.

| Sev | Category | Location | Issue | Suggested fix | Effort |
| --- | --- | --- | --- | --- | --- |
| 🟡 | architecture | `apps/web/app/(public)/agence/connexion/page.tsx:22` vs `apps/web/app/api/agence/signup/route.ts:34-37` | `claim_agence_grant` is a `SECURITY DEFINER` RPC that mutates state (`update public.logement_access_grants set agence_id = auth.uid() ...`, `supabase/migrations/0027_logement_access_grants_agence_claim.sql:60`) — exactly the "RPC métier" category INSTALL.md now says must go through a Route Handler (`aidd_docs/INSTALL.md:64`). The agence signup path does this correctly (Route Handler's `afterInsert` callback). But the agence connexion path calls the same RPC directly from the browser via `ConnexionForm`'s `afterSignIn` client callback (`supabase.rpc("claim_agence_grant", ...)`), never touching a Route Handler. The new shared `ConnexionForm` abstraction made it easy to smuggle a business mutation into what looks like a "just sign in" callback — the exact kind of new-module smell to watch for. | Add a small Route Handler (e.g. `POST /api/agence/claim-grant`) that wraps the RPC call server-side, and have `afterSignIn` in `agence/connexion/page.tsx` call that instead of invoking `supabase.rpc(...)` directly. | S |
| 🟢 | architecture | `apps/web/app/api/rendez-vous/[id]/route.ts` (whole file) | Now internally consistent — PATCH (`:6-96`) and DELETE (`:98-130`) both go through this handler, and `RendezVousRow.tsx` calls both via `fetch`. No further action; noted only to confirm the prior fix fully landed. | — | — |
| 🟢 | architecture | `aidd_docs/INSTALL.md:64`, `82`; `packages/logement/src/index.ts:1-2`, `packages/intervention/src/index.ts:1-3` | Docs still match reality: `packages/artisan` is gone from the folder tree, and the thin-adapter caveat for `logement`/`intervention` is documented and accurate (both packages are still 2-3 line re-exports; CRUD still lives in 40 `apps/web` files calling `.from(...)` directly, e.g. `apps/web/app/(owner)/proprietaire/espace/rappels/RappelRow.tsx`). This is the same known, deliberately-deferred gap from the prior audit — flagging only to confirm no fresh drift. | No new action; still a candidate for the deferred "extract packages/logement CRUD" work if ever prioritized. | — |

Boundary checks that passed clean: all 3 `connexion` pages (`artisan`, `proprietaire`, `agence`) use the shared `ConnexionForm`; both SIRET-based signup routes (`api/artisan/signup/route.ts`, `api/agence/signup/route.ts`) use `createSiretAccount`; `packages/logement`/`packages/intervention` line counts are unchanged (still 2-3 lines each); every one of the 36 `getServerSupabaseClient` call sites is a legitimate Route Handler/layout/server-page — `middleware.ts` and `lib/persona.ts`'s residual `createServerSupabaseClient` references are correct exceptions (middleware needs the `NextRequest`-cookie adapter shape, not `next/headers`; `persona.ts` only imports the type). The 5 previously-duplicated artisan pages (`page.tsx`, `profil/page.tsx`, `rendez-vous/page.tsx`, `recherche-adresse/page.tsx`, `interventions/nouvelle/page.tsx`) now contain zero auth-guard code, confirming the layout centralizes it correctly. The newest commit (`d0c55b5`, contact SIRET lookup) follows the documented convention exactly: `api/proprietaire/contacts/siret/route.ts` is a Route Handler for the external SIRET call, while `ContactForm.tsx`/`ContactRow.tsx` do plain single-table insert/update/delete directly against Postgres+RLS.
