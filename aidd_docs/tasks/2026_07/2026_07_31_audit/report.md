# Codebase Audit: alpha-cil (full 7-pillar run)

MVP feature-complete (14 user stories + 14 additional feature requests shipped, pre-production), RLS/SECURITY DEFINER security model is fundamentally sound, but zero automated test coverage and one real cross-tenant data leak need fixing before wider usage or a production deploy.

- **Date**: 2026-07-31
- **Scope**: full monorepo (`apps/web`, `packages/*`, `supabase/migrations`) — all 7 pillars
- **Health**: fair (one active security leak + a poor-rated tests pillar keep this out of "good"; findings are isolated/addressable, not systemic — no pillar shows widespread broken correctness)
- **Findings**: 8 critical, 24 warning, 24 minor (56 total) — corrected 2026-07-31: the original count miscounted 32/16 instead of 24/24; the table below (and the per-pillar files) were always correct, only this header summary was off.

## Findings

Sorted critical → warning → minor. Full per-pillar detail (including all minor rows) lives in the sibling files in this folder: `code-quality.md`, `architecture.md`, `security.md`, `dependencies.md`, `performance.md`, `tests.md`, `ui.md`.

| Sev | Category | Location | Issue | Suggested fix | Effort |
| --- | --- | --- | --- | --- | --- |
| 🔴 | security | `supabase/migrations/0022_dpe_detail_et_infos_logement.sql:170-179` (+ `0008_rattachement_logement.sql:66-74`) | `match_or_create_logement` is `grant execute to authenticated` with no caller-role check; any signed-up account can call it directly via PostgREST and harvest another owner's real email for any guessed/known address. Same leak class as the one fixed in `0018`. | Add a caller check (mirror `get_rendez_vous_owner_email`, `0030_rendez_vous.sql:196-205`) or stop returning `notify_email` from the RPC. | M |
| 🔴 | tests | `supabase/migrations/0018_acces_carnet_scope_interventions.sql:1-8` | The historical cross-tenant leak this migration fixed (owner attaching another owner's intervention to their own grant) has no regression test pinning it in place. | pgTAP/integration test: two owners, cross-grant attach attempt, assert reject. Highest-priority test in the repo. | M |
| 🔴 | tests | `supabase/migrations/0027_logement_access_grants_agence_claim.sql:22-64` (`claim_agence_grant`) | Token-hijack surface for agency access (row-locked, 6-branch authz) has zero coverage of any branch or of the concurrent double-claim race the lock exists to prevent. | Integration test per branch + a concurrency test firing two simultaneous claims. | M |
| 🔴 | tests | `supabase/migrations/0008_rattachement_logement.sql:22-94` (`match_or_create_logement`) | The single most powerful RLS bypass in the schema (artisan search/create across all logements) has no test on its ambiguous-match, notify-existing-owner, or bypass-scope-limits branches. | Integration tests: 0/1/2+ matches, claimed vs unclaimed, confirm no other access path reaches this data. | M |
| 🔴 | tests | `apps/web/lib/persona.ts:18-31` (`resolvePersona`) + each espace root guard | Role-guard is fully manual per-page (already needed one cross-persona fix, `0e5e193`); nothing automatically verifies all 3×3 persona/espace combinations still redirect correctly. | Playwright/route-handler matrix: 3 personas × 3 espace roots = 9 cases. | M |
| 🔴 | tests | `apps/web/app/api/{artisan,proprietaire,agence}/signup/route.ts` | Signup/SIRET-verification gate (the entry point for every account) has no coverage of invalid SIRET, gov-API-down, duplicate email, or orphaned-auth-user-on-insert-failure. | Unit-test each route with `verifySiret`/Supabase mocked per branch; at least one happy-path integration test per persona. | M |
| 🔴 | ui | `apps/web/app/(owner)/proprietaire/espace/layout.tsx:48`, `(agence)/agence/espace/layout.tsx:48`, every artisan page | Authenticated shell (`flex` + fixed `w-48` sidebar) never stacks on narrow viewports — ~100px left for main content on a phone. Hits all 3 personas' entire dashboards. | Add a responsive breakpoint (`flex-col sm:flex-row`), collapsible/tab nav below `sm`. | M |
| 🔴 | code-quality | `apps/web/app/page.tsx:42` | Homepage "Agence immobilière" card links to `/proprietaire/connexion` instead of `/agence/connexion` (copy-paste leftover) — agency visitors land on the wrong login form. | Fix the `href`. | S |
| 🟡 | security | `apps/web/app/api/cron/rappels-entretien/route.ts:31` | `Authorization: Bearer ${CRON_SECRET}` check fails open to literal `"Bearer undefined"` if the env var is ever unset — unlike `packages/db/client.ts`'s `requireEnv`, which fails closed for other secrets. | `if (!process.env.CRON_SECRET \|\| authorization !== ...)`. | S |
| 🟡 | security | `supabase/migrations/0007_artisans_update_self.sql:7-11` | Artisan's own-row update policy has no column restriction — an artisan can rewrite their own already-verified `siret` to any value, undermining the trust anchor for future interventions. | Restrict grant to column-level (`denomination, adresse, telephone, corps_metier`), drop blanket `update`. | S |
| 🟡 | architecture | `packages/logement/src/index.ts:1-2`, `packages/intervention/src/index.ts:1-3` | INSTALL.md documents these packages as owning CRUD/business logic; in reality they're thin external-API adapters and all CRUD (43 files) lives inline in `apps/web`. | Update INSTALL.md to match reality, or extract the repeated CRUD into the packages. | L |
| 🟡 | architecture | artisan espace (no `layout.tsx`) vs owner/agence's shared `layout.tsx` + `EspaceSidebar.tsx` | Same ~30-line auth-guard boilerplate copy-pasted across 5+ artisan pages instead of centralized — a future guard change (e.g. MFA) risks being missed on one page. | Add `apps/web/app/(artisan)/artisan/espace/layout.tsx` + `EspaceSidebar`, matching owner/agence. | M |
| 🟡 | architecture | `apps/web/app/(owner)/proprietaire/espace/calendrier/RendezVousRow.tsx:27-42` vs `:68-73` | `save`/`toggle` go through the `/api/rendez-vous/[id]` Route Handler (so business logic runs); `handleDelete` calls Supabase directly, skipping it. | Route delete through the same handler for consistency. | S |
| 🟡 | architecture | ~10 "use client" row/form components | Plain-CRUD mutations bypass Route Handlers and rely solely on RLS — a defensible but undocumented convention. | Document the two allowed write paths (Route Handler for side-effects, direct client+RLS for single-table CRUD) in INSTALL.md/CONTRIBUTING. | S |
| 🟡 | code-quality | `apps/web/app/(artisan)/artisan/espace/*/page.tsx` (5+ files) | No `layout.tsx` for the artisan persona — guard/chrome/nav duplicated per page; `profil/page.tsx` already lost its nav entirely as a result. | Add `layout.tsx` + `EspaceSidebar` for artisan, mirroring owner/agence. | M |
| 🟡 | code-quality | 34 files (route handlers, layouts, server pages) | `cookies()` + `createServerSupabaseClient(...)` adapter boilerplate copy-pasted verbatim in every one. | Add a `getServerSupabaseClient()` wrapper in `packages/db`/`apps/web/lib`. | M |
| 🟡 | code-quality | `apps/web/app/api/artisan/signup/route.ts` vs `.../agence/signup/route.ts` | ~90% identical signup flow (SIRET verify, `signUp`, error mapping). | Extract a shared `createSiretAccount(...)` helper. | M |
| 🟡 | code-quality | 3 persona `connexion/page.tsx` files | Byte-for-byte identical sign-in forms except title/redirect (agence adds invitation-claim). | Extract a shared `ConnexionForm({ persona, redirectPath, title, ... })`. | M |
| 🟡 | code-quality | `apps/web/app/(artisan)/artisan/espace/page.tsx:21-23`, `.../finances/page.tsx:8-10` | Local re-implementations of `corpsMetierLabel` (already exported from `apps/web/lib/corps-metier.ts`) with a subtly different null-handling fallback. | Import the shared helper, delete local copies. | S |
| 🟡 | code-quality | `apps/web/app/(owner)/proprietaire/espace/page.tsx:13-19`, `apps/web/app/LogementReadOnlyView.tsx:6-12` | `chauffageLabel`/`vmcLabel` duplicated identically in both files (no shared export exists yet, unlike `corpsMetierLabel`). | Add the exports to `apps/web/lib/equipements.ts`, import from both. | S |
| 🟡 | code-quality | 14-18 files | `Intl.DateTimeFormat`/`Intl.NumberFormat` "fr-FR" formatter constants redeclared identically everywhere. | Move to a shared `apps/web/lib/formatters.ts`. | S |
| 🟡 | code-quality | 13 API route files | `isNonEmptyString` reimplemented locally in every one instead of a shared module. | Move to `apps/web/lib/form-validation.ts`. | S |
| 🟡 | code-quality | `RappelRow.tsx:70-85`, `ContactRow.tsx:61-66`, `DevisRow.tsx:72-77`, `RendezVousRow.tsx:68-73` | `toggleTraite`/`handleDelete` ignore the returned Supabase `error`, unlike sibling `handleSave` — a failed delete/toggle fails silently and leaves the loading state stuck forever (button disabled, no retry short of reload). | Check the error, show the same `Alert` pattern as `handleSave`, reset loading state on failure. | S |
| 🟡 | dependencies | `apps/web/package.json` (`next@15.5.21`) | Hard-pins `postcss@8.4.31`, carrying 3 known advisories (path traversal / XSS); every `next` 15.x/16.x release pins the same version. | Add `pnpm.overrides["postcss"]: ">=8.5.18"`, re-verify build. | S |
| 🟡 | dependencies | `apps/web/package.json` (`next@15.5.21` → `sharp@0.34.5`) | 4 libvips CVEs, fixed only in `sharp>=0.35.0`; every `next` 15.x/16.x caps below it. Dormant today (`next/image` unused). | `pnpm.overrides["sharp"]: ">=0.35.3"` or drop the `sharp` build allowance until `next/image` is adopted. | S |
| 🟡 | dependencies | `apps/web/package.json` | `next` one major behind (`15.5.21` vs `16.2.12`). | Schedule the Next 16 upgrade once MVP work stabilizes. | M |
| 🟡 | dependencies | `packages/notifications/package.json` | `resend` two majors behind (`4.8.0` vs `6.18.1`), single call site (`mailer.ts`). | Review v5/v6 changelogs, bump. | S |
| 🟡 | dependencies | `apps/web/package.json`, `packages/ui/package.json` | `lucide-react` pre-1.0 vs `1.28.0` latest. | Bump, check for renamed/removed icon exports in use. | S |
| 🟡 | dependencies | `packages/ui/package.json` | `tailwind-merge` one major behind (`2.6.1` vs `3.6.0`), backs `cn()`. | Bump, spot-check class-merge output. | S |
| 🟡 | performance | all 33 `supabase/migrations/*.sql` | Zero `CREATE INDEX` statements anywhere — every RLS-filtered FK (`logement_id`, `artisan_id`, `projet_id`, `contact_id`, `agence_id`) forces a sequential scan; invisible today, will bite as any logement's history grows. | One migration adding indexes on every FK listed (incl. `logement_access_grant_interventions.intervention_id`). | S |
| 🟡 | performance | `packages/intervention/src/sirene.ts:36`, `.../ademe.ts:22`, `packages/logement/src/dpe.ts:37` | Bare `fetch(url)` to gov APIs, no timeout — a stalled upstream hangs until the platform's own function timeout. | Add `AbortSignal.timeout(5000)` to each call. | S |
| 🟡 | performance | `apps/web/app/api/artisan/interventions/route.ts:84,123` | `lookupDpe`/`verifyRge` are independent but awaited sequentially, doubling worst-case submission latency. | `Promise.all([...])`. | S |
| 🟢 | code-quality | `RendezVousRow.tsx:27-42` vs `ArtisanRendezVousRow.tsx:24-39` | Duplicated `patch()` fetch-wrapper. | Extract `usePatchRendezVous(id)` hook. | S |
| 🟢 | code-quality | owner vs agence `EspaceSidebar.tsx` | Identical component, differing only in hardcoded `LINKS`. | Accept a `links` prop, share one implementation. | S |
| 🟢 | architecture | `aidd_docs/INSTALL.md:82` | `packages/artisan` documented but never created. | Update INSTALL.md or extract the package. | S |
| 🟢 | architecture | 3 persona `SignOutButton.tsx` | Byte-for-byte identical except hardcoded redirect path. | Collapse into one component with a `redirectTo` prop. | S |
| 🟢 | security | `supabase/migrations/0025_devis_projets.sql:77-86` | `devis_insert_owner` checks `projet_id` ownership but not that `contact_id` also belongs to the same owner — same missing-cross-table-check shape as a previously real bug, low impact today since `contacts_select_owner` still blocks reads. | Add the cross-table `contact_id` ownership check. | S |
| 🟢 | dependencies | `apps/web/package.json` (`react-leaflet`) | Ships under Hippocratic-2.1 (non-OSI "ethical source" license) — not GPL, but non-standard for a commercial product. | Quick legal glance, or swap to a plain-MIT wrapper if blocking. | S |
| 🟢 | dependencies | transitive (`next` → `sharp` → `@img/sharp-win32-x64`) | Apache-2.0 + LGPL-3.0-or-later bundled libvips binary — standard for `sharp`, low risk. | No action; note for record. | S |
| 🟢 | dependencies | all workspace `package.json` (`typescript`) | `5.9.3` vs `7.0.2` latest (native compiler rewrite) — large jump, dev-only. | Defer until TS7 tooling/plugin ecosystem matures. | S |
| 🟢 | dependencies | `packages/db`, root, `apps/web` `package.json` | Trivial safe patch bumps available (`@supabase/supabase-js`, `@supabase/ssr`, `turbo`, `postcss` devDep). | Bump in a routine refresh PR. | S |
| 🟢 | performance | 5 list pages (finances, calendrier, projets, export, historique) | No `.range()`/`.limit()` anywhere — full unbounded result sets loaded every render. Not urgent at current data volume. | Add pagination once histories grow past ~1-2 screens. | M |
| 🟢 | performance | `apps/web/app/(owner)/proprietaire/espace/LogementMap.tsx:29` | Re-geocodes the address client-side on every mount instead of using the already-persisted lat/lon. | Persist lat/lon on `logements` (already done at creation/edit), only re-geocode as a null-fallback. | S |
| 🟢 | performance | `.../finances/page.tsx:4`, `FinancesCharts.tsx:1` | `recharts` imported directly rather than via `next/dynamic` — Next's route splitting already isolates it, so this is a minor missed optimization. | Wrap in `next/dynamic(..., { ssr: false })`. | S |
| 🟢 | tests | `packages/intervention/src/{sirene,ademe}.ts`, `packages/logement/src/dpe.ts` | Direct `fetch` to live gov endpoints, no fixture/mock seam — any future test either hits the network or invents ad-hoc mocking. | Add a fetch-injection seam + recorded fixture JSON per endpoint. | M |
| 🟢 | tests | `packages/notifications/src/mailer.ts:11-19` | Differing failure-handling contracts per call site (silent-swallow vs explicit error) are untested; RGPD-relevant as the sole consent-notification channel. | Unit test each route with `sendMail` mocked to throw. | S |
| 🟢 | tests | 50 `CREATE POLICY` / 26 SECURITY DEFINER occurrences across 19+14 migration files | No pgTAP/SQL-level harness; every policy's negative case (user B seeing/writing user A's row) is unverified beyond the app layer happening to call Supabase correctly. | Stand up `supabase test db` (pgTAP) for the 3 highest-value isolation cases. | L |
| 🟢 | tests | project-wide, no `.github/` | Not even `build`/`lint` run automatically on push/PR. | Minimal GitHub Actions workflow running `pnpm build`+`pnpm lint` on PR. | S |
| 🟢 | tests | `apps/web/app/api/proprietaire/export/pdf/route.ts` | Carnet PDF export (closest thing to an RGPD data-portability flow) has no content or access-control test. | Structural/snapshot test of generated PDF + auth test for non-owner export attempt. | S |
| 🟢 | tests | `apps/web/app/api/cron/rappels-entretien/route.ts` | No test for idempotency (double-trigger) or correct due-only scoping. | Unit test with a due/not-due fixture set across two owners. | S |
| 🟢 | tests | whole monorepo | No test runner installed anywhere (`vitest`/`jest`/`@playwright/test` absent from every `package.json`). | Pick one stack, wire a `test` script into `turbo.json` before the first test file. | S |
| 🟢 | ui | 5 delete/revoke actions (`ContactRow`, `DevisRow`, `RappelRow`, `RendezVousRow`, `RevokeButton`) | Fire immediately on click with no confirmation, inconsistent with the `window.confirm()` pattern already used in the export flow for a lower-stakes case. | Reuse the same `window.confirm()` pattern on all 5. | S |
| 🟢 | ui | artisan persona nav (4 pages + missing on `profil/page.tsx`) | Hand-duplicated nav using plain `<a href>` (full page reload) instead of Next `Link`, unlike owner/agence's shared, client-routed sidebar. | Extract `EspaceSidebar` + shared `layout.tsx` for `(artisan)`. | M |
| 🟢 | ui | `CalendrierList.tsx:134-152` | Filter-chip buttons toggle state visually only (gradient fill) with no `aria-pressed`. | Add `aria-pressed={activeKinds.has(kind)}`. | S |
| 🟢 | ui | checkboxes/radios across 5+ forms | Raw unstyled native inputs next to fully-themed `Input`/`Select`/`Button` on the same forms — `packages/ui` has no `Checkbox`/`Radio`. | Add a themed `Checkbox`/`Radio` to `packages/ui`, or at least `accent-color` the existing ones. | S |
| 🟢 | ui | 2 file-upload fields (`DecennaleForm.tsx`, `InterventionForm.tsx`) | Reuse the text-tuned `Input` component; inconsistent native file-picker chrome, no size/format hint. | Add a lightweight `FileInput` variant to `packages/ui`. | S |

## Top actions

1. **Close the `match_or_create_logement` email leak** (security 🔴) — real, live cross-tenant data exposure reachable today via PostgREST. Fix before any wider usage. → refactor/impeccable.
2. **Fix the mobile-unusable dashboard shell** (ui 🔴) — every persona's entire authenticated area is effectively broken on a phone. → refactor.
3. **Fail closed on `CRON_SECRET`** (security 🟡, S effort) — one-line fix, closes an "unset env var = anyone can trigger the cron" hole. → refactor.
4. **Fix the homepage agence login link** (code-quality 🔴, S effort) — one-line, real user-facing bug. → refactor.
5. **Pin the two historical/high-power RLS bypass functions with tests** (`match_or_create_logement`, `claim_agence_grant`) plus the persona-guard matrix (tests 🔴×3) — these are the functions where a silent regression would be most damaging and least visible. → test.
6. **Add DB indexes on every RLS-filtered FK** (performance 🟡, S effort, one migration) — cheap insurance against every list view degrading as soon as real usage starts. → refactor.
7. **Give the artisan persona a shared `layout.tsx`/`EspaceSidebar`** (flagged independently by code-quality, architecture, and ui) — closes the biggest source of cross-page drift risk in the codebase (already caused one dropped-nav bug). → refactor.
8. **Add a minimal CI workflow** (`pnpm build` + `pnpm lint` on PR) (tests 🟢, S effort) — the floor beneath which any future test suite, or just basic regression safety, would sit. → refactor.
9. **Override `postcss`/`sharp` via `pnpm.overrides`** (dependencies 🟡, S effort each) — closes 4 known CVEs (currently dormant but free to fix). → refactor.
10. **Add confirmation on the 5 unconfirmed delete/revoke actions** (ui 🟡, S effort) — cheap, prevents accidental data loss for real users. → refactor.

## Coverage

- **Scanned**: code-quality, architecture, security, dependencies, performance, tests, ui
- **Skipped**: none

## Update 2026-07-31 (same day): all 24 warnings fixed

All 8 criticals were fixed first (see the top of this file / memory), then all 24 🟡 warning-severity rows above were closed in the same session:

- **Security**: `CRON_SECRET` now fails closed on an unset env var; `artisans_update_self` restricted to column-level grants (migration `0035`) so an artisan can never rewrite their own verified `siret`.
- **Performance**: migration `0036` adds indexes on every RLS-filtered FK across all 33 prior migrations; `AbortSignal.timeout(5000)` added to the SIRET/RGE/DPE gov-API fetches; `lookupDpe`/`verifyRge` now run via `Promise.all` in the artisan intervention route.
- **Architecture**: `RendezVousRow`'s delete now goes through a new `DELETE /api/rendez-vous/[id]` handler instead of a direct client call; the artisan persona got its own `layout.tsx` + `EspaceSidebar.tsx` (its `profil`/`interventions/nouvelle` pages were simplified to match the same convention every other espace page already followed); INSTALL.md corrected to describe `packages/logement`/`packages/intervention` as the thin external-API adapters they actually are (not CRUD owners), `packages/artisan` dropped from the docs (never built), and the two allowed write paths (Route Handler vs direct client+RLS) now documented.
- **Code-quality**: `corpsMetierLabel`/`chauffageLabel`/`vmcLabel` de-duplicated into their existing/new shared modules; 4 Row components (`RappelRow`, `ContactRow`, `DevisRow`, `RendezVousRow`) now check the returned error on delete/toggle instead of failing silently with a stuck loading state; new `apps/web/lib/formatters.ts` and `apps/web/lib/form-validation.ts` replace ~30 duplicated local `Intl` formatter and `isNonEmptyString` definitions across 16 and 13 files respectively; new `apps/web/lib/supabase-server.ts` (`getServerSupabaseClient()`) replaces the cookies()+client boilerplate across 33 files; new `apps/web/lib/siret-signup.ts` (`createSiretAccount()`) shares the artisan/agence signup flow; new `apps/web/app/ConnexionForm.tsx` shares all 3 persona connexion pages (agence's invitation-claim step plugged in via an `afterSignIn` callback).
- **Dependencies**: `pnpm-workspace.yaml` now overrides `postcss`/`sharp` to their patched versions (closes the 4 known CVEs); `resend` bumped 4→6, `lucide-react` bumped to 1.x, `tailwind-merge` bumped to 3.x. The `next` 16 major bump was deliberately left for a dedicated pass — not urgent pre-launch and carries real App Router migration risk.

Every fix was verified against the real test suite (16 unit + 14 integration + 9 Playwright e2e, all passing), a full `tsc --noEmit`, and a full `next build` — not just written and assumed correct. Only the 24 🟢 minor-severity rows remain open.
